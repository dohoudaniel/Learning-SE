#!/usr/bin/python3
def number_keys(a_dictionary):
    """ A function that returns the number of keys in a dictionary """
    if not a_dictionary:
        return None
    
    daniel = len(a_dictionary)
    return daniel
    
    """
    daniel = len(a_dictionary.keys())
    return daniel
    """

