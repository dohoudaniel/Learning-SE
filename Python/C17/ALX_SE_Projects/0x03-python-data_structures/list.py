from time import sleep as delay

myList = [1, 4, 9, 16, 25]
print(myList)

# Indexing returns the item in the requested index
index0 = myList[0]

# Slicing a list returns a new index
sliced = myList[-3:]
print(sliced)
print("Hello, World")
