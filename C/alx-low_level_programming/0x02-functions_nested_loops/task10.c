#include <stdio.h>
#include "main.h"

/**
 * add - Adds two numbers and returns the result
 * Return: The result of the two numbers.
*/
int add(int A, int B)
{
    int M;
    M = A + B;

    return (M);
}
